<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/content_submit_comment"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.amzgolinski.yara.ui.SubmitCommentFragment"
    tools:showIn="@layout/activity_submit_comment">

  <TextView
      android:id="@+id/submit_comment_submission_title"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"/>

  <View
      android:id="@+id/divider"
      style="@style/Divider"
      android:layout_below="@+id/submit_comment_submission_title"/>

  <EditText
      android:id="@+id/submit_comment_text"
      android:layout_width="match_parent"
      android:layout_height="match_parent"
      android:gravity="top"
      android:inputType="textMultiLine"
      android:layout_below="@id/divider"
      android:layout_marginTop="8dp"
      android:layout_alignParentStart="true"/>

  <ProgressBar
      style="?android:attr/progressBarStyle"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:gravity="center"
      android:layout_marginTop="72dp"
      android:layout_centerHorizontal="true"
      android:layout_below="@id/divider"
      android:visibility="gone"
      android:id="@+id/submit_comment_progress"/>

</RelativeLayout>
